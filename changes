step 1
add a history table to security master data reference
include log date and time, user id, before values and after values also add a edit reason to history table and include that field on the data entry screen

step 2
add a status to the master securities table how some of the master securities in
pre-issue
active
matured
defaulted
retired

Add this as a drop down to the table and insert values for each status based on real data from the API you already build

add lineage to the master data and master_security_history table so you can track changes
And pinpoint who changed what 

step 3
Create another tab for 
Security Master History
Include the full security master table 
and have an export all from the security master latest 
table and an export all for the history table 
Export should be to a CSV file and an Excel file

allow you select USER who changed it on history table, ticker,currency, exchange ISIN as drop 

also on the history table if you select one of the records show lineage from the start of the change